/* 
 *  24 / 02 / 2026
 *  Estoy aprendiendo a usar los punteros (*) y las referencias (&), 
 *  entonces le pedí a gemini este ejercicio que trato de resolver en 
 *  este código. 
 *
 *  Imagina que tienes dos cajas, valorA y valorB. Queremos que lo que 
 *  está en A pase a B y viceversa.
 */ 

// Bibliotecas
#include <iostream>
#include <cmath>
    // #include <vector>
    // #include <iomanip>

// Declaración de constantes globales
// --- Constantes Fundamentales (SI) ---
constexpr double hPlanck{6.62607015e-34};  // J * s
constexpr double hBarra{hPlanck / (2.0 * M_PI)};  // J * s
constexpr double masaElectron{9.1093837e-31};  // kg
constexpr double cargaElemental{1.60217663e-19};  // C
constexpr double velocidadLuz{299792458.0};  // m / s
constexpr double constanteBoltzmann{1.380649e-23};  // J / K
// --- Unidades Atómicas (a.u.) ---
// En este sistema, hBarra = masaElectron = cargaElemental = 1
constexpr double radioBohr{5.29177210e-11};  // m (1 a.u. de longitud)
constexpr double energiaHartree{4.35974472e-18}; // J (1 a.u. de energía)                                                 
// --- Factores de Conversión (Útiles para TD-DFT/CIS) ---
constexpr double hartreeAEv{27.211386245};  // De Hartrees a electrón-Voltios 
constexpr double hartreeAKcalMol{627.509474};  // De Hartrees a kcal/mol
constexpr double evAnm{1239.84193};  // Para calcular longitudes de onda (lambda = 1239.84 / E_ev)


// Declaración de Funciones 
void intercambiarValor(int&, int&);


// Programa Principal
int main() 
{
    int caja1, caja2;

    std::cout << "El valor de caja 1 es " << caja1 << std::endl;
    std::cout << "El valor de caja 2 es " << caja2 << std::endl;

    std::cout << "\n IntercambiandoValores de las cajas \n" << std::endl;
    intercambiarValor(caja1, caja2);

    std::cout << "El valor de caja 1 es " << caja1 << std::endl;
    std::cout << "El valor de caja 2 es " << caja2 << std::endl;
}; // main

// Implementación de funciones
void intercambiarValor(int& a, int& b)
{
    int c{a};
    a = b;
    b = c;

};


/*  ------------------------ Notas del programa ------------------------
 *  
 *
 *
 *  ------------------------------ Teoría ------------------------------
 *
 *
 *
 */
